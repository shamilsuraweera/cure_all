openapi: 3.0.3
info:
  title: Cure-All API
  version: 0.1.0
  description: OpenAPI skeleton for Cure-All backend.
servers:
  - url: http://localhost:3000
paths:
  /health:
    get:
      summary: Health check
      responses:
        "200":
          description: OK
  /auth/login:
    post:
      summary: Login
      responses:
        "200":
          description: OK
